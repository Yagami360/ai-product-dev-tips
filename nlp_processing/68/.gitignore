# ローカル環境変数（HF トークン等の秘密情報を含みうるため git 管理外）
.env

# 公式リポジトリ（README の手順で clone する外部リポジトリ。この Tip では管理しない）
SensorLLM/

# uv 仮想環境
.venv/

# ダウンロードしたモデル・学習成果物（大容量）
ckpt_1EE1/
chronos_t5_base/
out_stage1/
out_stage2/
*.part

# Python キャッシュ
__pycache__/
*.pyc
mHealth*.log

# 推論結果などのバイナリ出力（大容量・再生成可能なので git 管理しない）。出力は outputs/ 以下へ
outputs/
*.npy
*.npz

# Stage2 サンプル別図は出力ディレクトリ扱い（全 16 枚は再生成可能）。
# README 掲載の代表 2 枚のみ git add -f で明示追跡する。
images/stage2_samples/

# 検証用にコピーした NAB データ(nlp_processing/70 由来)
nab/
run_multi.py
anomaly_test.py
anomaly_indomain.py
MHEALTHDATASET/
data/
mh*.ipynb
whole_data/
