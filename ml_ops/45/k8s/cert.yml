# ManagedCertificate (カスタムリソース定義) / Google マネージド証明書のマニフェスト
apiVersion: networking.gke.io/v1beta2
kind: ManagedCertificate
metadata:
  name: graph-cut-api-cert  # ManagedCertificateリソース名を指定
spec:
  domains:
    - yagami360.com         # 取得したドメイン名を指定
    